# Colin Flutter 앱 개발 경량 하네스

- 공통 규칙은 상위 `AGENTS.md`를 따른다. 자동 포스팅 중 `CLAUDE.md` 전체를 읽지 않는다.
- 언어: 한국어 직접체 `~다`. 실제 구현 실패와 판단을 짧게 넣는다.
- 주제: Flutter 앱 개발 실전, IoT, 스마트홈, BLE, MQTT, AWS IoT, GetX/Riverpod, 테스트, 운영.
- 현재 흐름: Flutter IoT/스마트홈 앱과 테스트·상태관리 실전. 최근 포스트 제목과 겹치지 않게 이어 쓴다.
- SEO: `Flutter`, 핵심 패키지명, 문제 상황을 title·description·첫 문단에 넣는다.
- 태그: 첫 태그는 `Flutter`. 후보는 `Dart`, `BLE`, `MQTT`, `AWS IoT`, `GetX`, `Riverpod`, `CleanArchitecture`, `flutter_blue_plus`, `mqtt5_client`, `fl_chart`, `Firebase`, `Android`, `iOS`, `CI/CD`, `IoT`, `스마트홈`.
- 코드 글은 코드 바로 위에 왜 필요한 코드인지 한 줄을 둔다. 코드 중심 글만 1,600~2,200자까지 허용한다.
- 시각 자료: 아키텍처 흐름도, 상태 변화 표, 패키지 비교표, 디버깅 로그 전후 비교를 우선한다.
- 웹 검색은 기본 생략한다. Flutter 공식 문서나 패키지 최신 API 확인이 꼭 필요할 때만 1회 확인한다.
